<%= link_to 'Edit', edit_bookmark_path(@bookmark) %> |
<%= link_to 'Back', bookmark_path %> |
<%= link_to 'Leave a comment', new_bookmark_comment_path(@bookmark) %>

<ul class="list-group">
  <% @bookmark.reviews.each do |bookmark| %>
    <li class="list-group-item"><%= bookmark.content %></li>
  <% end %>
</ul>
